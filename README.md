# SaaS платформа управления франчайзинговой сетью

## 📋 Описание проекта

Мультитенантная SaaS-платформа для управления франчайзинговой сетью мебельной компании (на примере divan.ru, но масштабируемо на другие сети). Платформа позволяет систематизировать работу дилеров в регионах, контролировать KPI, автоматизировать маркетинг и масштабировать лучшие практики.

## 👥 Типы пользователей

- **Франчайзер** (управляющая компания): управляет сетью, задает стандарты, контролирует KPI
- **Дилеры** (франчайзи в регионах): выполняют задачи, формируют отчеты, управляют продажами

## 🛠 Технологический стек

### Frontend
- Next.js 14 (App Router)
- React 18 + TypeScript
- Redux Toolkit + RTK Query
- Ant Design (UI библиотека)
- Recharts (графики)
- Tailwind CSS (стили)

### Backend
- Go 1.22+
- Gin фреймворк
- PostgreSQL + Redis
- JWT аутентификация
- Docker контейнеры

## 🚀 Быстрый старт

### Требования
- Node.js 18+
- Go 1.22+
- Docker & Docker Compose
- PostgreSQL

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd franchise-saas-platform

# Установка зависимостей frontend
cd frontend
npm install

# Установка зависимостей backend
cd ../backend
go mod tidy

# Запуск через Docker
docker-compose up --build
```

### Переменные окружения

Создайте файлы `.env.local` в соответствующих директориях с необходимыми переменными.

## 🏗 Архитектура проекта

```
franchise-saas-platform/
├── frontend/                 # Next.js приложение
│   ├── src/
│   │   ├── components/       # Переиспользуемые компоненты
│   │   ├── pages/            # Страницы приложения
│   │   ├── store/            # Redux store
│   │   ├── services/         # API сервисы
│   │   └── types/            # TypeScript типы
│   ├── public/               # Статические файлы
│   └── package.json
├── backend/                  # Go API сервер
│   ├── cmd/
│   │   └── server/           # Точка входа
│   ├── internal/
│   │   ├── handlers/         # HTTP обработчики
│   │   ├── services/         # Бизнес логика
│   │   ├── models/           # Модели данных
│   │   ├── middleware/       # Middleware
│   │   └── database/         # Работа с базой данных
│   ├── migrations/           # SQL миграции
│   └── go.mod
├── docker-compose.yml        # Docker конфигурация
├── Dockerfile.*              # Docker файлы
└── README.md
```

## 📊 Основные модули

1. **Аутентификация и авторизация** - мультиролевая система
2. **Чек-лист дилера** - интерактивный список задач
3. **CRM и воронка продаж** - управление лидами
4. **Маркетинговая автоматизация** - публикации, контент
5. **Панель франчайзера** - статистика сети, управление дилерами
6. **Отчеты и аналитика** - KPI, сравнения, прогнозы

## 🔐 Безопасность

- JWT токены с refresh токенами
- Защита от XSS, CSRF, SQL инъекций
- Rate limiting
- Аудит действий пользователей
- GDPR совместимость

## 🌍 White-label возможности

- Возможность смены цветовой схемы
- Загрузка собственного логотипа
- Локализация интерфейса
- Персонализированные шаблоны

## 📞 Поддержка

Для вопросов и поддержки обращайтесь в службу поддержки.