# SaaS-платформа управления франчайзинговой сетью

Мультитенантная SaaS-платформа для управления франчайзинговой сетью мебельной компании. Платформа предназначена для систематизации работы дилеров в регионах, контроля KPI, автоматизации маркетинга и масштабирования лучших практик.

## 🏗️ Архитектура

- **Фронтенд:** Next.js 14 (React 18 + TypeScript), App Router, Redux Toolkit, Ant Design
- **Бэкенд:** Go 1.22+, Gin Framework, PostgreSQL, Redis
- **Инфраструктура:** Docker, Docker Compose, Nginx

## ✨ Основные возможности

- ✅ Мультиролевая система (франчайзер, дилеры, менеджеры)
- ✅ White-label решение с возможностью смены брендинга
- ✅ Адаптивный дизайн для desktop/tablet/mobile
- ✅ Модульная архитектура с чётким разделением ответственности
- ✅ Поддержка мультитенантности
- ✅ Готовность к интеграции с внешними сервисами (VK, Avito, 2GIS)

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Node.js 18+ (для локальной разработки фронтенда)
- Go 1.22+ (для локальной разработки бэкенда)

### Установка и запуск

#### Вариант 1: С использованием Docker (рекомендуется)

```bash
# 1. Клонируйте репозиторий
git clone <url-репозитория>
cd franchise-saas-platform

# 2. Запустите проект через Docker Compose
docker-compose up --build

# Приложение будет доступно по адресам:
# - Фронтенд: http://localhost:3000
# - Бэкенд API: http://localhost:8080
# - PostgreSQL: localhost:5432
# - Redis: localhost:6379
```

#### Вариант 2: Локальная разработка

**Запуск бэкенда:**

```bash
cd backend

# Установка зависимостей
go mod download

# Настройка переменных окружения
export DB_HOST=localhost
export DB_PORT=5432
export DB_USER=postgres
export DB_PASSWORD=postgres
export DB_NAME=franchise_db
export JWT_SECRET=your_secret_key

# Запуск сервера
go run cmd/server/main.go
```

**Запуск фронтенда:**

```bash
cd frontend

# Установка зависимостей
npm install

# Настройка переменной окружения
echo "NEXT_PUBLIC_API_URL=http://localhost:8080" > .env.local

# Запуск в режиме разработки
npm run dev
```

**Запуск базы данных (через Docker):**

```bash
docker-compose up postgres redis
```

## 📁 Структура проекта

```
/workspace/
├── frontend/                 # Next.js приложение
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── pages/           # Страницы Next.js
│   │   ├── store/           # Redux store и слайсы
│   │   ├── services/        # API сервисы
│   │   ├── types/           # TypeScript типы
│   │   └── styles/          # Глобальные стили
│   ├── public/              # Статические файлы
│   └── package.json
│
├── backend/                  # Go бэкенд
│   ├── cmd/
│   │   └── server/          # Точка входа приложения
│   ├── internal/
│   │   ├── handlers/        # HTTP обработчики
│   │   ├── services/        # Бизнес-логика
│   │   ├── models/          # Модели данных
│   │   ├── middleware/      # Middleware
│   │   └── database/        # Работа с БД
│   ├── migrations/          # SQL миграции
│   ├── config/              # Конфигурация
│   └── go.mod
│
├── docker-compose.yml        # Docker Compose конфигурация
├── Dockerfile.frontend       # Dockerfile для фронтенда
├── Dockerfile.backend        # Dockerfile для бэкенда
├── nginx.conf               # Конфигурация nginx
└── README.md                # Этот файл
```

## 🔧 Конфигурация

### Переменные окружения

**Бэкенд:**

```env
PORT=8080                    # Порт сервера
DB_HOST=postgres            # Хост базы данных
DB_PORT=5432                # Порт базы данных
DB_USER=postgres            # Пользователь БД
DB_PASSWORD=postgres        # Пароль БД
DB_NAME=franchise_db        # Название базы данных
JWT_SECRET=your_secret_key  # Секретный ключ для JWT
```

**Фронтенд:**

```env
NEXT_PUBLIC_API_URL=http://localhost:8080  # URL бэкенд API
```

## 📚 API Документация

### Аутентификация

#### POST /api/v1/auth/register
Регистрация нового пользователя

```json
{
  "email": "user@example.com",
  "password": "securepassword",
  "role": "dealer",
  "tenant_id": "tenant-123",
  "first_name": "Иван",
  "last_name": "Иванов"
}
```

#### POST /api/v1/auth/login
Вход в систему

```json
{
  "email": "user@example.com",
  "password": "securepassword"
}
```

**Ответ:**
```json
{
  "user": { ... },
  "token": "jwt_token",
  "refresh_token": "refresh_token"
}
```

### Пользователи

#### GET /api/v1/users/profile
Получение профиля текущего пользователя (требуется аутентификация)

#### PUT /api/v1/users/profile
Обновление профиля

```json
{
  "first_name": "Иван",
  "last_name": "Петров",
  "phone": "+7 (999) 123-45-67"
}
```

### Чек-листы

#### GET /api/v1/checklists
Получение всех чек-листов пользователя

Параметры запроса:
- `page`: Номер страницы (по умолчанию: 1)
- `limit`: Элементов на страницу (по умолчанию: 10)

#### POST /api/v1/checklists
Создание нового чек-листа

```json
{
  "title": "Ежедневные задачи",
  "description": "Задачи на сегодня",
  "tasks": [
    {
      "title": "Позвонить клиенту",
      "description": "Позвонить важному клиенту",
      "order": 1
    }
  ]
}
```

## 🛠️ Разработка

### Запуск тестов

**Бэкенд:**
```bash
cd backend
go test ./...
```

**Фронтенд:**
```bash
cd frontend
npm run test
```

### Линтинг

**Фронтенд:**
```bash
cd frontend
npm run lint
```

### Сборка production версии

```bash
# Сборка обоих сервисов
docker-compose -f docker-compose.yml build

# Или по отдельности
cd frontend && npm run build
cd backend && go build -o main cmd/server/main.go
```

## 🔒 Безопасность

- ✅ HTTPS only (TLS 1.3)
- ✅ Хеширование паролей (bcrypt)
- ✅ Защита от XSS, CSRF, SQL injection
- ✅ Rate limiting
- ✅ Audit log всех действий
- ✅ JWT аутентификация с refresh токенами

## 🐛 Решение проблем

### Проблема: Не удаётся подключиться к базе данных

**Решение:**
1. Убедитесь, что PostgreSQL запущен: `docker-compose ps`
2. Проверьте переменные окружения в `docker-compose.yml`
3. Проверьте логи: `docker-compose logs postgres`

### Проблема: Ошибка CORS при запросах к API

**Решение:**
1. Убедитесь, что `NEXT_PUBLIC_API_URL` указывает на правильный адрес
2. Проверьте настройки CORS в `backend/cmd/server/main.go`
3. Убедитесь, что фронтенд и бэкенд запущены

### Проблема: Ошибки при сборке Go

**Решение:**
```bash
cd backend
go mod tidy
go mod download
```

### Проблема: Ошибки при установке npm пакетов

**Решение:**
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

## 📝 Лицензия

MIT

## 👥 Авторы

Franchise SaaS Team

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Добавил крутую функцию'`)
4. Запушьте ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, пожалуйста:
- Откройте issue в GitHub
- Напишите на email: support@franchise-saas.com


# 🚀 Быстрый старт - Пошаговая инструкция

## Перед началом

### Требования:
- ✅ Docker Desktop установлен и запущен
- ✅ Порты 3000, 8080, 5432, 6379 свободны
- ✅ Минимум 4GB свободной оперативной памяти

## Шаг 1: Применение исправлений

### 1.1. Удалите дублирующийся файл
```bash
cd /путь/к/проекту
rm -f backend/internal/handlers/auth.go
```

### 1.2. Замените файлы на исправленные версии

Замените содержимое следующих файлов:

**Backend:**
- `backend/cmd/server/main.go`
- `backend/internal/database/db.go`
- `backend/internal/handlers/auth_handler.go`
- `backend/internal/handlers/user_handler.go`
- `backend/internal/handlers/checklist_handler.go`

**Конфигурация:**
- `docker-compose.yml`
- `README.md`

## Шаг 2: Очистка старых контейнеров

```bash
# Остановить все запущенные контейнеры проекта
docker-compose down

# Удалить все контейнеры (опционально)
docker-compose rm -f

# Очистить volumes (если нужно начать с чистой БД)
docker volume rm $(docker volume ls -q | grep franchise) 2>/dev/null || true
```

## Шаг 3: Запуск проекта

```bash
# Сборка и запуск всех сервисов
docker-compose up --build

# Или в фоновом режиме:
docker-compose up --build -d
```

### Ожидаемый вывод при успешном запуске:

```
✓ Container franchise-postgres  Started
✓ Container franchise-redis     Started  
✓ Container franchise-backend   Started
✓ Container franchise-frontend  Started
```

## Шаг 4: Проверка работоспособности

### 4.1. Проверка всех сервисов
```bash
docker-compose ps
```

Все сервисы должны быть в состоянии `Up`:
```
NAME                    STATUS
franchise-postgres      Up (healthy)
franchise-redis         Up (healthy)
franchise-backend       Up
franchise-frontend      Up
```

### 4.2. Проверка логов

**Проверка Backend:**
```bash
docker-compose logs backend | grep "Сервер запущен"
```

Должно быть:
```
Сервер запущен на порту 8080
```

**Проверка подключения к БД:**
```bash
docker-compose logs backend | grep "Успешное подключение"
```

Должно быть:
```
Успешное подключение к PostgreSQL
```

### 4.3. Тестирование API

**Health check:**
```bash
curl http://localhost:8080/health
```

Ответ:
```json
{"status":"OK","service":"franchise-saas-backend"}
```

**Тест регистрации:**
```bash
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "admin123",
    "role": "franchiser",
    "tenant_id": "main-network",
    "first_name": "Администратор",
    "last_name": "Системы"
  }'
```

Успешный ответ (status 201):
```json
{
  "user": {
    "id": "...",
    "email": "admin@example.com",
    "role": "franchiser",
    ...
  },
  "token": "eyJ...",
  "refresh_token": "eyJ..."
}
```

## Шаг 5: Открытие приложения

### Фронтенд
Откройте браузер и перейдите по адресу:
```
http://localhost:3000
```

Вы должны увидеть страницу входа.

### Вход в систему
Используйте данные, которые вы создали при регистрации:
- Email: `admin@example.com`
- Пароль: `admin123`

## Шаг 6: Создание тестовых данных

### Создание дилера:
```bash
# Сначала получите токен администратора из предыдущего шага
TOKEN="ваш_токен_здесь"

curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "dealer@example.com",
    "password": "dealer123",
    "role": "dealer",
    "tenant_id": "main-network",
    "first_name": "Иван",
    "last_name": "Петров"
  }'
```

## 🐛 Решение проблем

### Проблема 1: Порт уже занят
```
Error: port is already allocated
```

**Решение:**
```bash
# Найти процесс на порту 8080
sudo lsof -i :8080

# Остановить процесс
sudo kill -9 PID

# Или измените порт в docker-compose.yml:
# "8081:8080" вместо "8080:8080"
```

### Проблема 2: PostgreSQL не запускается
```
postgres  | could not create shared memory segment
```

**Решение:**
```bash
# Увеличьте лимиты в Docker Desktop
# Settings -> Resources -> Advanced
# Memory: минимум 4GB
```

### Проблема 3: Frontend не подключается к Backend
```
Failed to fetch
```

**Решение:**

1. Проверьте, что Backend запущен:
```bash
curl http://localhost:8080/health
```

2. Проверьте CORS в логах Backend:
```bash
docker-compose logs backend | grep CORS
```

3. Если нужно, пересоберите Frontend:
```bash
docker-compose up --build frontend
```

### Проблема 4: Ошибка при подключении к БД
```
failed to ping database
```

**Решение:**
```bash
# Подождите, пока PostgreSQL полностью запустится
docker-compose logs postgres | grep "ready to accept"

# Перезапустите Backend
docker-compose restart backend
```

### Проблема 5: Ошибка "permission denied"
```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перезапустите терминал или выполните
newgrp docker
```

## 📊 Мониторинг в реальном времени

### Просмотр логов всех сервисов:
```bash
docker-compose logs -f
```

### Просмотр логов конкретного сервиса:
```bash
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f postgres
```

### Проверка использования ресурсов:
```bash
docker stats
```

## 🛑 Остановка проекта

### Остановка с сохранением данных:
```bash
docker-compose stop
```

### Полная остановка и удаление контейнеров:
```bash
docker-compose down
```

### Остановка + удаление всех данных (БД будет очищена):
```bash
docker-compose down -v
```

## ✅ Чеклист успешного запуска

- [ ] Все 4 контейнера в статусе `Up`
- [ ] Health check возвращает `{"status":"OK"}`
- [ ] PostgreSQL показывает "ready to accept connections"
- [ ] Backend показывает "Сервер запущен на порту 8080"
- [ ] Frontend доступен на http://localhost:3000
- [ ] Успешная регистрация через API
- [ ] Успешный вход через веб-интерфейс

## 🎯 Следующие шаги

После успешного запуска вы можете:

1. **Изучить API документацию** - см. `APIDOCS.md`
2. **Создать тестовые данные** - чек-листы, задачи
3. **Настроить окружение** - переменные среды
4. **Начать разработку** - добавить новые функции

## 📞 Получение помощи

Если что-то не работает:

1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что применены все исправления
3. Проверьте, что удалён файл `auth.go`
4. Создайте issue с полным выводом ошибки

## 💡 Полезные команды

```bash
# Перезапуск конкретного сервиса
docker-compose restart backend

# Пересборка без кеша
docker-compose build --no-cache

# Просмотр переменных окружения
docker-compose config

# Выполнение команды внутри контейнера
docker-compose exec backend sh
docker-compose exec postgres psql -U postgres -d franchise_db

# Просмотр сетей
docker network ls | grep franchise

# Очистка неиспользуемых ресурсов
docker system prune -a
```

---

**Готово!** 🎉 