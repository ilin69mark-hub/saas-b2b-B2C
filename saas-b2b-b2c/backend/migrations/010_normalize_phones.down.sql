-- Migration: 010_normalize_phones (DOWN)
-- Откат миграции 010 не нужен — мы не знаем, в каком формате были телефоны
-- до нормализации (этот скрипт идемпотентен только в одну сторону).
-- Данный файл оставлен пустым, чтобы структура миграций была парной.
SELECT 1;
